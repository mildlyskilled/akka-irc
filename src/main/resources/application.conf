//#chatserver
chatserver {
  #take defaults from common configuration
  include "common"
  akka {
    remote.netty.ssl {
      hostname = "172.19.0.246"
      #hostname = "193.61.28.44" #(BBK LAN address )
      #port = 1099 BBK Configuration
      port = 1099 
    }
  }
}
//#chatserver

//#client
chatclient {
  include "common"
  akka {
    # this would normally be overridden at run time
  	remote.netty.ssl.hostname="172.19.0.246" #allows us to hard code as well (?)
    remote.netty.ssl.port = 2554
  }
}
